# Matching UI (FastAPI)

```bash
# desde la raíz convocaur/
pip install fastapi uvicorn
python web/api.py
# → http://127.0.0.1:8765
```

En Windows también: `web\lanzar.bat`.

Lee rankings precalculados en `data/processed/matching/`.
Para regenerarlos: `python scripts/run_match.py`.
